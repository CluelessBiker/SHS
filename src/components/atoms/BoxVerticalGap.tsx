import styled from 'styled-components';

const BoxVerticalGap = styled.div`
  display: flex;
  text-align: center;
  align-items: center;
  gap: var(--spacing-2);
  flex-direction: column;
`;

export default BoxVerticalGap;
