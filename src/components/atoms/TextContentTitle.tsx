import styled from 'styled-components';

const TextContentTitle = styled.h3`
  text-align: center;
  text-transform: uppercase;
  font-size: var(--typography-size-title-lg);
`;

export default TextContentTitle;
